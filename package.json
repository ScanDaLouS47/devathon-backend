{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "create-db": "php artisan migrate --seed",
        "rollback-db": "php artisan migrate:rollback",
        "reset-db": "php artisan migrate:reset",
        "start-server": "php artisan serve"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
