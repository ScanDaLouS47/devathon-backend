{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "create-db": "php artisan migrate",
        "create-db-seed": "php artisan db:seed",
        "start-server": "php artisan serve",
        "rollback-db": "php artisan migrate:rollback"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
